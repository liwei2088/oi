#include <cstdio>
#include <iostream>
using namespace std;
const int N = 100005;
int n, x[N], y[N];
int main() {
    scanf("%d", &n);
    for (int i = 1; i <= n; i++) {
        scanf("%d%d", &x[i], &y[i]);
    }

    return 0;
}