#include <iostream>
using namespace std;
const int N = 100005;
int n, x[N], y[N];
int main() {
    cin >> n;
    for (int i = 1; i <= n; i++) {
        cin >> x[i] >> y[i];
        
    }
    return 0;
}